[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    pcai: E2E contra los endpoints REALES de PCAI (NIM LLM + embeddings). No corre en
        CI; requiere VPN, tokens vigentes y el stack levantado. Activar con: pytest -m pcai
